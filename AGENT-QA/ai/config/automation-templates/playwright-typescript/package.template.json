{
  "name": "{{project_slug}}-{{story_id_slug}}-playwright",
  "version": "1.0.0",
  "private": true,
  "description": "Automatizacion Playwright TypeScript generada para {{story_id}} - {{story_title}}.",
  "scripts": {
    "postinstall": "playwright install chromium",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "typescript": "^5.5.0"
  }
}
